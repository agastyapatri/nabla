"""
Plotting utilities for the training process

Requisite Functionalities:
    1. Find a way to show the predictions converging to the global minimum of the loss function
    2. Find a way to plot the training + validation performance of the target neural network
    3. 

"""
import matplotlib.pyplot as plt 
import numpy as np 
import scipy as sp
